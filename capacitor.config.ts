import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ultimoproyectofinal',
  appName: 'ultimoproyectofinal',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
